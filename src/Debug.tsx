/*import { Reply } from "./types";

import { Reply } from "../storyteller/types";

type DebugProps = {
  reply: Reply;
};

export function Debug({ reply }: DebugProps) {
    /*if (!reply.debug.description || !reply.debug.direction) {
        return (
            <div>
                <pre>No message.</pre>
            </div>
        );
    }

    return (
        <div>
            <h3>Description</h3>
            <pre>{reply.debug.description}</pre>
            <h3>Directions</h3>
            <pre>{reply.debug.direction}</pre>
            <pre>{JSON.stringify(reply.directions, null, 4)}</pre>
            <h3>Details</h3>
            <pre>{reply.debug.details.instructions}</pre>
            <pre>{JSON.stringify(reply.debug.details.response, null, 4)}</pre>
        </div>
    );/
    return (
        <div>
            <h3>Nonsens</h3>
            <pre>{reply.player}</pre>
        </div>
    );
}*/